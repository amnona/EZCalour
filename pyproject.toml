[tool.commitizen]
name = "cz_conventional_commits"
version = "1.6.0"
tag_format = "v$version"
version_files = [
    "ezcalour_module/__init__.py:__version__",
    "setup.py:version"
]
update_changelog_on_bump = true
